[package]
name = "application"
version = "0.1.0"
edition = "2024"

[dependencies]
domain = { path = "../domain" }
ports = { path = "../ports" }
thiserror = "2.0.11"

[dev-dependencies]
async-trait = "0.1.89"
tokio = { version = "1.0", features = ["macros", "rt-multi-thread"] }
